// 패킷추가는 위에서 아래로 순서대로 추가하세요.

include "id.fbs";
include "../Common/Common.fbs";

namespace Protocol.C2S;

table ReqLogin
{
	a : float;
	vec : [Common.Vector3];
	pos : [Common.vecPosition];
	b: int;
}

table ReqTestMsg
{
	str : string;
	a : int;
	aaa: Common.Temp;
}
