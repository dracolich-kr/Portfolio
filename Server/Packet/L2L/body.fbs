// 패킷추가는 위에서 아래로 순서대로 추가하세요.

include "id.fbs";
include "../Common/Common.fbs";

namespace Protocol.L2L;

table ReqLogin
{
	id : eID = ReqLogin;
	a : int;
	b : Common.Vector3;
	c : int;
}

table StartServer
{
	id : eID = StartServer;
	a : int;
}